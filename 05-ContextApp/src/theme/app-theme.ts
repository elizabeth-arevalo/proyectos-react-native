import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        padding: 20,
        marginTop: 10,
        flex: 1,
    },

    tittle: {
        fontSize: 20,
        fontWeight: "bold",
        color: "#000",
    },

    PrimaryButton: {
        backgroundColor: "#dadada",
        padding: 10,
        borderRadius: 5,
        marginVertical: 10,
    },
});
